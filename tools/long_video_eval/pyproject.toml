[project]
name = "long-video-eval"
version = "0.1.0"
description = "Config-driven long text-to-video evaluation suite"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
setup = ["huggingface_hub>=0.24.0"]

[project.scripts]
long-video-eval = "long_video_eval.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["long_video_eval*"]
